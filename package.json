{
  "name": "quintasoft-website",
  "version": "2.0.0",
  "description": "Quintasoft marketing website — bilingual (EN/ES) static single-page site",
  "private": true,
  "scripts": {
    "build": "npm run build:css && npm run build:js",
    "build:css": "esbuild css/styles.css --minify --outfile=css/styles.min.css --allow-overwrite",
    "build:js": "cat js/i18n.js js/main.js | esbuild --loader=js --minify > js/app.min.js",
    "serve": "python3 -m http.server 8080"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
